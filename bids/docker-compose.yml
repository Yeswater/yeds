# 已迁移至仓库根目录
#
# 请使用（在 yeds 仓库根）：
#   cp .env.example .env
#   ./deploy/scripts/build-bids-jars.sh
#   docker compose -f docker-compose.yml -f docker-compose.macos.yml up -d
#
# 详见 env/构建环境-全栈Docker模式.md
