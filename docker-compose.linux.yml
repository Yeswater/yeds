# Linux（含 Docker Engine）：合并使用
# docker compose -f docker-compose.yml -f docker-compose.linux.yml up -d
services:
  bids-config:
    extra_hosts:
      - "host.docker.internal:host-gateway"
  bids-exec:
    extra_hosts:
      - "host.docker.internal:host-gateway"
  bids-web:
    extra_hosts:
      - "host.docker.internal:host-gateway"
